---
type: edit
priority: P1
expected_output: both
---

# Mini-PR — Rating 4.7→4.8 + extracción a `shared/businessProfile.ts`

## Objetivo

Dejar de mentir con `4.7` en schemas (rating real Google Maps = **4.8**) y **eliminar el problema de fondo**: ~40 hardcodes distribuidos por el código. Todos leerán de una sola fuente en `shared/businessProfile.ts`.

Esto también adelanta parte de PR D3 del plan GEO (extracción a shared businessProfile), así que cuando llegue S3 y montemos el cron + API Google Business Profile, solo habrá 1 sitio donde enchufarlo.

## Contexto

Confirmado por Ivan el 2026-04-22: rating real en Google Maps = **4.8**.

Tu último reporte identificó estos puntos de cambio:
- `server/seoInjector.ts:1472` — aggregateRating baseline 307 reviews + rating hardcoded
- `client/src/config/seo-config.ts` — rating hardcoded (varios sitios)
- `client/src/pages/BoatDetailPage.tsx` — rating hardcoded
- `client/src/pages/category-*.tsx` — rating hardcoded en todas las category pages
- Commit `2cbb406` — propagó `4.7` a traducciones i18n (hay que revertir o actualizar el valor)

Total estimado: ~40 puntos, 0 deseados después de este PR.

## Tareas

### Tarea 1 — Inventario completo

Antes de editar nada: `grep -rn "4.7" --include="*.ts" --include="*.tsx" --include="*.json" --include="*.html" .` y filtrar los que sean rating (descartar coincidencias no-rating como versions, coordinates, etc.).

Adjuntar el inventario al reporte. Si detectas hardcodes de rating en archivos que no sean los 4 sospechosos de arriba, listarlos.

### Tarea 2 — Crear `shared/businessProfile.ts`

Si ya existe un archivo equivalente (`shared/businessInfo.ts`, `shared/businessConstants.ts`, etc.), usa ese en vez de crear uno nuevo. Si no existe, crea `shared/businessProfile.ts`:

```typescript
// Single source of truth para datos del negocio que aparecen en schemas,
// meta tags y UI. Cambia aquí → cambia en todos los puntos.
//
// CUÁNDO ACTUALIZAR:
// - Rating/review count: cuando cambie en Google Maps
//   (manual hasta S3 donde se automatiza vía GBP API).
// - Coords/address/phone/openingHours: solo si cambia el negocio.
//
// REGLA: nunca hardcodear estos valores fuera de este archivo.

export const BUSINESS_RATING = 4.8;
export const BUSINESS_REVIEW_COUNT = 307; // valor actual en código — verificar en siguiente PR
export const BUSINESS_RATING_LAST_UPDATED = '2026-04-22';

// Si detectas otras constantes frecuentemente hardcoded (lat/lng, teléfono,
// sameAs, horario), añádelas también aquí con su comentario.
```

Si el archivo hay que crearlo: path exacto dependerá de la convención del repo (`shared/` vs `src/shared/` vs otro). Usa la que ya se use en otros `shared/*` del proyecto.

### Tarea 3 — Refactorizar los ~40 hardcodes

Cada uno de los puntos detectados en Tarea 1 debe pasar de `4.7` a `BUSINESS_RATING` importado desde `shared/businessProfile.ts` (o la ruta equivalente).

Mismo trato si encuentras el review count `307` hardcoded en varios sitios → importa `BUSINESS_REVIEW_COUNT`.

Para el commit i18n `2cbb406`:
- Si los archivos i18n tienen strings tipo `"ratingValue": "4.7"` → actualizarlos a `"4.8"`.
- Si los archivos i18n no tienen el rating inline y lo leen de una constante, no hace falta tocarlos.
- NO revertir el commit entero si hay otros cambios legítimos dentro; solo el valor del rating.

### Tarea 4 — Smoke test post-cambio

Después del refactor, corre en local (o dime cómo prefieres) y verifica:

```bash
# Levantar el server local
npm run dev  # o equivalente

# En otra terminal:
curl -s http://localhost:PORT/es/ | grep -oE '"ratingValue":"[^"]*"'
curl -s http://localhost:PORT/en/ | grep -oE '"ratingValue":"[^"]*"'
curl -s http://localhost:PORT/es/barco/astec-400 | grep -oE '"ratingValue":"[^"]*"'
curl -s http://localhost:PORT/es/alquiler-barcos-tossa-de-mar | grep -oE '"ratingValue":"[^"]*"'
```

Todos deben devolver `"4.8"`. Si alguno devuelve `"4.7"`, queda un hardcode sin refactorizar — reportarlo.

### Tarea 5 — Commit + push

**Un solo commit**, mensaje sugerido:

```
refactor(seo): extract rating+reviewCount to shared/businessProfile.ts + update 4.7→4.8

- New shared/businessProfile.ts as single source of truth
- Replace ~40 hardcoded 4.7 across seoInjector, seo-config, BoatDetailPage, category-*, i18n
- Rating now 4.8 (Google Maps verified 2026-04-22)
- Prep for S3 GBP API auto-sync
```

Push a `origin/main` como hiciste con los quick wins GEO previos.

## Formato del reporte

```
## Inventario
- Total hardcodes 4.7 encontrados: N
- Breakdown por archivo: <tabla>

## Cambios aplicados
- Archivo creado: shared/businessProfile.ts (N líneas)
- Archivos refactorizados: N — <lista>
- Archivos i18n actualizados: N — <lista>

## Smoke test
- /es/              → "4.8" ✅ | "4.7" ❌
- /en/              → ...
- /es/barco/astec-400 → ...
- /es/alquiler-barcos-tossa-de-mar → ...

## Commit
- Hash: <sha>
- Files changed / +lines / -lines

## Observaciones
<cualquier hallazgo inesperado: rating duplicado en otro lado, review count inconsistente, etc.>
```

## Reglas

- **Un solo commit limpio.** Si algo falla, reportar, no hacer commits parciales.
- **No tocar nada fuera del scope** (no renombrar, no mover archivos, no reformat masivo).
- Si descubres que el review count también varía entre archivos (p.e. algunos tienen `307`, otros `312`), reportarlo al final — lo vemos en PR aparte.
- Si el smoke test local falla por razón no relacionada con el rating (p.e. server no arranca), reportar sin forzar.
