node_modules
dist
dist/prerendered/
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables (contain secrets)
.env
.env.local
.env.*.local

# NPM cache (local)
.npm-cache

# Claude Code config (local)
.claude/

# LibreOffice / OpenOffice lockfiles
.~lock.*#

# Playwright MCP local debug logs (console captures, screenshots)
.playwright-mcp/

# Playwright e2e artifacts (auth state has session cookies; results are transient)
e2e/.auth/
test-results/
playwright-report/

# Local backups (never commit *.md.bak etc.)
*.bak

# Claude Code project MCP config (contains MCP bearer token)
.mcp.json